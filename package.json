{
  "name": "miabega",
  "version": "0.1.0",
  "private": true,
  "description": "Miabé Ga — plateforme d'épargne collaborative (monorepo web + mobile + Supabase)",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "web": "pnpm --filter @miabega/web dev",
    "mobile": "pnpm --filter @miabega/mobile start"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
